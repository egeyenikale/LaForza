# Browser-safe public configuration
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:4000/api/v1
NEXT_PUBLIC_CHAIN_ID=31337

# Backend configuration
HOST=127.0.0.1
PORT=4000
LOG_LEVEL=info
CORS_ORIGIN=http://localhost:3000

# One-command local demo
CHAIN_RPC_URL=http://127.0.0.1:8545
CHAIN_ID=31337
DATA_DIR=./.data

# The backend defaults to Hardhat account #0 only for the local demo.
# Never reuse that public test key or commit funded keys for a live network.
